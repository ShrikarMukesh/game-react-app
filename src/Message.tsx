function Message() {
    return (
        <h1>Hello world</h1>
    )
}

export default Message;
// This is a simple React component that renders a heading with the text "Hello world".
// It uses the default export syntax, allowing it to be imported in other files.
// The component is defined as a function named "Message" that returns a JSX element.
// The component can be used in a React application to display a message on the screen.
// The component is exported as the default export of the module, allowing it to be imported in other files.